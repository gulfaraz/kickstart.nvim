-- live-command previews macros and the norm command
-- https://github.com/smjonas/live-command.nvim

return {
  'smjonas/live-command.nvim',
  config = function()
    require('live-command').setup()
  end,
}
